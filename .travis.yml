
language: node_js
node_js: stable
    
before_install:
- git config --global user.name "sasswen"
- git config --global user.email "962041678@qq.com"

- npm install -g hexo-cli

#安装hexo及插件
install:
  - npm install  

#清除 生成
script:
  - hexo g
